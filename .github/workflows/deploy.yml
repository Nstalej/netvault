name: Deploy to App Instance

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy NetVault
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          # IMPORTANT: this host must be the VM/LXC where NetVault runs,
          # not the Proxmox hypervisor node.
          host: ${{ secrets.DEPLOY_HOST || '172.17.0.56' }}
          username: ${{ secrets.DEPLOY_USER || secrets.PROXMOX_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY || secrets.PROXMOX_SSH_KEY }}
          script_stop: true
          script: |
            cd ~/projects/netvault
            git pull origin main
            docker compose build --no-cache
            docker compose up -d
            sleep 10
            curl -f http://localhost:8080/health || exit 1
